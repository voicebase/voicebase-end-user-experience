import Base from 'react-player/lib/players/Base';

import { propTypes, defaultProps } from './props'

export default class VbsBase extends Base {
  static propTypes = propTypes;
  static defaultProps = defaultProps;
}
